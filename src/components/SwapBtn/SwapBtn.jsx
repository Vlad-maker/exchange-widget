import "./SwapBtn.scss";

function SwapBtn() {
  return (
    <button
      // onClick={() => setCurrenciesPosition(!currenciesPosition)}
      className="swap__btn"
      type="button"
    ></button>
  );
}

export default SwapBtn;
